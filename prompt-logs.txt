## Prompt Log

All prompts sent to the AI assistant, appended after each phase.

---

### Phase 1 — 2026-06-24

**Session start prompt:**
> READ prompt.md STRICTLY FOLLOW IT AND ASK ME IF YOU HAVE ANY DOUBTS. Start phase 1.

**Approved plan:** Phase 1 implementation plan covering project scaffold, migrations for `users` and `reimbursements`, CFO seed, auth endpoints (register/login/logout), and `authenticate` middleware. Cookie strategy: plain signed cookie via `cookie-parser`.

---

### Phase 2 — 2026-06-24

**Prompt:**
> Start phase 2

**Built:**
- `authorize` middleware factory (role-list based, policy in route declarations)
- `POST /rest/roles/assign` — CFO-only; validates userId exists and role is valid
- `GET /rest/employees` — EMP blocked (403), RM sees own reports, APE sees EMP+RM, CFO sees all
- `POST /rest/employees/assign` — CFO-only; validates empId is EMP and rmId is RM
- `DELETE /rest/employees/assign` — CFO-only; validates pair is actually linked before unlinking

---

### Phase 3 — 2026-06-24

**Prompt:**
> Start Phase 3

**Built:**
- `POST /rest/reimbursements` — EMP-only; validates title required, amount > 0
- `PATCH /rest/reimbursements/:id` — RM/APE/CFO dispatch:
  - RM: scope-checked (own reports only), rm_status must be PENDING
  - APE: rm_status must be APPROVED, ape_status must be PENDING; records ape_id
  - CFO: ape_status must be APPROVED, cfo_status must be PENDING
  - Double-acting prevented at all stages
- `deriveEmpStatus(r)` — single named function for EMP-facing status derivation

---

### Phase 4 — 2026-06-24

**Prompt:**
> start Phase 4

**Built (Commit A — read side):**
- `GET /rest/reimbursements` — EMP (own+derived status), RM (pending from reports), APE (rm-approved/ape-pending), CFO (ape-approved)
- `GET /rest/reimbursements/:userId` — RM views subordinate's full claim history; guards: exists, is EMP, is direct report
- Empty lists always return `{ reimbursements: [] }`, never null or 404

**Built (Commit B — edge cases + README):**
- Edge-case audit: all 15 items from spec §5 reviewed
- EMP blocked on GET /employees at route layer (authorize middleware), not service
- README: data model decisions, all assumptions documented, setup instructions
